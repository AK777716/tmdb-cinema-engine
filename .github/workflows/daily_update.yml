name: Daily Indian & Global Movie Update

on:
  schedule:
    - cron: '0 2 * * *'
  workflow_dispatch: 

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'

      - name: Install dependencies
        run: |
          # ADDED: gspread, gspread-dataframe, and google-auth
          pip install pandas google-cloud-bigquery requests pandas-gbq python-dotenv gspread gspread-dataframe google-auth

      - name: Run Indian-Focus Pipeline
        env:
          TMDB_API_KEY: ${{ secrets.TMDB_API_KEY }}
          GCP_PROJECT_ID: ${{ secrets.GCP_PROJECT_ID }}
          BQ_DATASET: ${{ secrets.BQ_DATASET }}
          BQ_TABLE: ${{ secrets.BQ_TABLE }}
          GOOGLE_APPLICATION_CREDENTIALS: ${{ github.workspace }}/gcp_key.json
        run: |
          echo '${{ secrets.GCP_SA_KEY }}' > gcp_key.json
          # UPDATED: Added 'scripts/' prefix to match your folder structure
          python scripts/full_pipeline.py